import { Outlet } from 'react-router-dom';

const UserInfoRoute = () => {
    return (
        <div>
            <div>UserInfoRoute</div>
            <Outlet />
        </div>
    );
};

export default UserInfoRoute;
